# Synthetic CLI reference sample (pipeline tests)

## **config firewall policy**

set name "example"
set action accept

## **execute ping**

options 8.8.8.8

## **diagnose sniffer packet**

filter ...
